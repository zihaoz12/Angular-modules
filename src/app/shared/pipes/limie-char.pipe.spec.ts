import { LimieCharPipe } from './limie-char.pipe';

describe('LimieCharPipe', () => {
  it('create an instance', () => {
    const pipe = new LimieCharPipe();
    expect(pipe).toBeTruthy();
  });
});
